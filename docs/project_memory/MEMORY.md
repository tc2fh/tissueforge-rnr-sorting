# Memory index

- [Native active-motility drive — Python add_noise port COMPLETE](native-active-motility-drive.md) — DONE 2026-06-13 (Phase 3): the active self-propulsion drive is now NATIVE in the C++ fork feat/native-rnr-reconnection; the Python add_noise_active injection is retired to a comparison fallback. Per-cell Body::director + active-Brownian rotational diffusion evolved once/step at top of MeshSolver::preStepStart + per-vertex active force v0·⟨incident directors⟩ folded into VertexForce. SEAM: vertex integrator is OVERDAMPED (tf_engine_advance.cpp dx=dt·f·imass) w/ vertex mass=1 (density0⇒default MeshParticleType mass⇒imass=1,μ=1) ⇒ a FORCE v0·⟨n⟩ gives displacement dt·v0·⟨n⟩ = oracle dt·motility EXACTLY (engine wraps PBC ⇒ no %L; live adjacency ⇒ no _rebuild_incidence). API MeshSolver.set_motility(v0,Dr,seed)/get_motility_v0/get_motility_dr + body.director (SWIG; rm wrap.cxx to regen); v0=0 default=off. Gates: calibration probe_native_calibration.py (disp ratio 1.000/cos 1.000⇒μ=1; rot-diff rate∝Dr ratio 2.08), rate native M=4 37≈active35 M=6 146≈141 STABLE, pixi run test=49 (+test_native_motility.py), science σ-ordered+demixed-holds. PORTING_NOTES §6o. Completes the deliverable: RNR+active sorting runs entirely in TissueForge. [[active-motility-not-thermal-noise]] [[native-orientation-repair-faithful]] [[tf-swig-subi-needs-forced-regen]]
- [Oracle noise is ACTIVE MOTILITY, not thermal — clamp-free fix](active-motility-not-thermal-noise.md) — RESOLUTION 2026-06-11 (NOW NATIVE, see [[native-active-motility-drive]]): the §6j/§6l noise clamp was a band-aid over the WRONG NOISE MODEL. 3DVertVor/Manning does NOT use thermal Brownian noise — its thermal line Run.cpp:1344 (cR·ndist, √dt) is COMMENTED OUT; live line :1345 is x+=dt·motility (ACTIVE self-propulsion, dt-scaled, motility=temperature·⟨cell director⟩, directors rotate w/ Dr=1). Per-step disp ≤ dt·v0 ≈ 0.1×Lth (sub-Lth) → plain instantaneous-edge trigger (Reconnection.cpp:34 <Lth=1e-3) catches collapses with NO clamp. §6f's tf.Force.random matched long-time diffusion D=μ·kT but NOT per-step displacement (what the trigger needs) → 14–45× Lth/step → starvation → clamp band-aid. FIX: probe_active_motility.py + sort_periodic_oracle.py NOISE_MODEL=active (default). Rate restored clamp-free: M=4 35 (=clamped, vs starved 1), M=6 141, STABLE; v0=0 ctrl 38 (recon are deterministic-relaxation-driven; noise must NOT sabotage trigger). Gate test_clampfree_reconnection.py; pixi run test=47; PORTING_NOTES §6n. Supersedes [[clamp-enables-reconnection-not-just-stability]]; corrects [[thermal-noise-destabilizes-reconnection]]. [[native-orientation-repair-faithful]]
- [Clamp ENABLES reconnection — RESOLVED by active motility](clamp-enables-reconnection-not-just-stability.md) — 2026-06-11 (RESOLVED by [[active-motility-not-thermal-noise]]): OBSERVATIONS hold (unclamped THERMAL noise starves native I↔H: ~1 recon/3000 vs ~24 clamped, M=4; mechanism = √dt kicks 14–45× Lth blow edges past the Lth trigger), but the CONCLUSION "clamp needed / no clamp-free path" is WRONG — the oracle uses active motility (sub-Lth/step), caught with no clamp. Still-valid here: DP(Sahu Eq.2)=−demixing_index; DP_max(M=6)=0.56; area-demix σ-orders fast, count-DP lags→ensemble; Fig 1E/1F RESULTS (thermal+clamp) reproduce under active (being re-run). [[native-orientation-repair-faithful]] [[oracle-comparison-ceiling-physical]]
- [Native orientation repair = FAITHFUL fix, supersedes noise clamp](native-orientation-repair-faithful.md) — RESOLVED 2026-06-11: implemented 3DVertVor's orientation repair (oracle stabilizer #3) natively — a per-Body orientSign (abs signed volume + flip parity when V<0, applied in volume AND VolumeConstraint force; = flipping all polygonDirections_ together). Replaces the §6j Python noise-clamp DEPARTURE with the paper's own mechanism: faithful σ=0.5/Lth=1e-3/kT=0.1 sort STABLE 20000 steps with NO clamp; counter-test (repair off via env TF_VERTEX_NO_VOLUME_REPAIR=1) reproduces the exact −1.78 eversion. §6c's "volume robustness isn't the fix" was about a per-FACE abs misimplementation (overcounts→inflates); the per-CELL flip is the real fix. Deep Lth=0.05 departure still needs the trust-region (repair converts evert→inflate, isolating the two modes). pixi run test=45; PORTING_NOTES §6k. [[periodic-sort-noise-overshoot-fixed]] [[native-instability-is-displacement-overshoot]]
- [Periodic SORT reconnection blow-up — RESOLVED (noise overshoots Lth gap)](periodic-sort-noise-overshoot-fixed.md) — RESOLVED 2026-06-10: periodic sort (reconnection ON + thermal noise) everted a cell on the first reconnection. Factorial showed it needs BOTH recon AND noise; placement is already min-image (kickoff suspect #1 FALSE). Cause: DISP_STD=sqrt(2μ·kT·dt)=0.0141 ≈ 14× the post-reconnection Lth=1e-3 gap → one noise kick throws a fresh vertex past its neighbour → transient (self-recovering) eversion. Fix: per-vertex trust-region clamp on the NOISE (0.4×min-image nn-dist; binds only on Lth-scale fresh vertices), topology cached/vectorised. 20000 steps/84 reconnections STABLE (worst_min=0.270=initial min); D −0.079→−0.089 (modest, doesn't break −0.10 ceiling at σ=0.5 — recon rate is the limiter). Gate test_periodic_dynamics.py (+2 tests, incl. clamp-OFF→UNSTABLE load-bearing check); pixi run test=44; PORTING_NOTES §6j. Science open: sigma/Lth sweeps. [[periodic-substrate-engine-bug]] [[winding-clamp-stabilizes-sort]]
- [Periodic substrate blow-up — RESOLVED (was FlatSurfaceConstraint, not engine)](periodic-substrate-engine-bug.md) — RESOLVED 2026-06-10: the P5 σ=0 periodic-foam blow-up was NOT an MD-engine cutoff/ghost bug (that 06-09 diagnosis was wrong — "stable cutoff" was a radius>cell FREEZE; real force is cutoff+radius invariant). Cause: every SurfaceType auto-binds FlatSurfaceConstraint (default lam=0.1) whose force used a RAW centroid−vertex offset → box-sized for wrap faces → ≈1/dt force inverts wall cells. Fix: min-image it (meshRelativePosition) in actors/tfFlatSurfaceConstraint.cpp (no-op when periodic_geometry off). σ=0/σ=1/σ=1+noise foam now STABLE at large cutoff (noise alive); dynamic gate test_periodic_dynamics.py; pixi run test=42. Sort still breaks on reconnection (separate, [[native-instability-is-displacement-overshoot]]). PORTING_NOTES §6i
- [Periodic vertex-mesh geometry verified](periodic-geometry-verified.md) — 2026-06-01: mesh.periodic_geometry slice (tf_mesh_metrics min-image) VERIFIED — straddling cube area/vol=interior (flag OFF=box-spanning), Volume/SurfaceArea FORCES match interior to float32, native RNR edge=min-image, native I↔H round-trip across boundary OK, AND (P4, new) build_periodic_voronoi: a space-filling Kelvin packing (no free surface, adjacency wraps) via ghost-tiling+min-image dedup; FOAM BOX MUST = UNIVERSE BOX (engine min-images at Universe::dim; sub-box silently box-spanning); periodicity via GEOMETRY not coord-wrapping; gates test_periodic_geometry.py (P4 pack + sub-box guard) + native roundtrip (39 green); PORTING_NOTES §6g (geometry) + §6h (P4 generator); P0–P4 done, resume at P5 (min-image metrics + sort_faithful_3dvertvor in periodic bulk), see docs/periodic_mesh_kickoff_plan.md §9
- [Thermal noise destabilizes reconnection — PARTLY SUPERSEDED](thermal-noise-destabilizes-reconnection.md) — 2026-06-01 (cause corrected 2026-06-11 by [[active-motility-not-thermal-noise]]): observation holds (tf.Force.random thermal noise MONOTONICALLY worsens stability+sorting at sigma=1) but the cause is NOT "reconnection robustness vs kinetic trapping" — thermal Brownian (√dt) is the WRONG noise model (14–45× Lth/step, sabotages the trigger); the oracle uses active motility (dt-scaled, sub-Lth). The "tf.Force.random IS faithful" claim is WRONG for reconnection (only for long-time diffusion). Harness: rnr/scripts/sort_faithful_3dvertvor.py
- [Adhesion force is ALREADY area tension (Phase-G premise issue)](adhesion-force-is-already-area-tension.md) — CRITICAL 2026-06-01: TF Body-Adhesion FORCE = −∇(λ·A_het) (area tension) already; only its energy() is perimeter and energy() is never used by the integrator (force-based dynamics). So the planned σ·A actor is force-identical to Adhesion(λ=σ) and can't "beat" the energy-gate-OFF baseline. Real ceiling = stability (cell inverts ~step 12-14k), not tension type. Verified cos≈0.9997 vs −2∇A
- [Native instability is a displacement overshoot — RESOLVED](native-instability-is-displacement-overshoot.md) — RESOLVED 2026-05-31 (NO volume code touched): check-clamp-native failure was TWO displacement overshoots (A: periodic-image WRAP of corner cluster; B: post-reconnection STORM at every-step Lth=0.45), not winding/volume. FIXED by centring the cluster (Fix A) + a new native reconnect_interval throttle knob (oracle dtr=10dt) at Lth=0.4 INT=10 (Fix B); also fixed a parallel-implement scheduler RACE (targets miss shared verts/bodies) with a serial reconnection executor. HARDENED gate (min_vol>0 AND max_vol<3*V0, catches uniform inflation) PASSES; pixi run test = 32 green. Phase-E volume robustness stays reverted (was a misdiagnosis). See PORTING_NOTES §6e
- [Phase D native doQuality wired; Phase E premise DISPROVEN](phaseD-native-doquality-wired-blocked-by-E.md) — native RNR Phase D done (committed 6d67617): reconnection runs as active pass inside live doQuality via stockQualityOps isolation + enforceTrigger Condition-2 recheck; pixi run test 31 green; fixed mesh.quality=q double-free in tfMesh.i. UPDATE: Phase E (robust/abs volume) attempted & DISPROVEN — gate failure is a displacement overshoot not a winding flip, see [[native-instability-is-displacement-overshoot]]
- [Phase B native walk + conditions done](phaseB-native-walk-conditions-done.md) — native RNR Phase B done (committed 888f07c): C++ port of topology+conditions walk/vetoes + 3 JSON diagnostics, test_native_reconnection.py 6 tests, pixi run test 25 green; CRITICAL for Phase D: (1) stock MeshQuality collapses segfault on a finite Kelvin block even with reconnect_length=0 (run reconnection WITHOUT stock collapses); (2) build reconnection dep-chains SERIALLY (dense graph races parallel constructChains)
- [TF SWIG sub-.i needs forced regen](tf-swig-subi-needs-forced-regen.md) — CMake/Ninja only tracks top-level tissue_forge.i; editing a sub-.i or %included header does NOT re-run SWIG (build "succeeds" but new methods never reach Python); rm the generated tissue_forgePYTHON_wrap.cxx to force regen; FloatP_t is float32 (0.1→0.10000000149)
- [Phase G: new σ·A surface-tension actor](phaseG-new-surface-tension-actor.md) — Phase G het drive = a NEW native per-type-pair σ·A surface-tension actor (near-clone of SurfaceAreaConstraint::force area-gradient + type-pair gate); user constraint: do NOT modify/delete the existing Adhesion actor; done AFTER the reconnection port (A–F)
- [Winding clamp stabilizes sort](winding-clamp-stabilizes-sort.md) — Python per-vertex displacement clamp (operator.stable_step, rel_frac<0.5) prevents the sign-flip from Python; sort becomes STABLE (min_vol +2.09 vs old −218) AND DEEPER (D −0.10 vs −0.096); instability was never doing the sorting; pixi run check-clamp gates it
- [Oracle comparison: ceiling is physical](oracle-comparison-ceiling-physical.md) — Step-4 3DVertVor oracle (built VTK-free) periodic bulk does NOT break our D≈−0.10 ceiling at matched σ=1 (both ~0.06–0.10, plateau); deeper σ crashes its reconnection (winding degeneracy ~ our sign-flip); real unlock = native winding repair, not periodic/bigger
- [Faithful instability is winding sign-flip (PARTLY CORRECTED)](faithful-instability-is-winding-signflip.md) — observations hold (negatives ARE winding flips, V_geo>0, energy gate sorts) but the ROOT-CAUSE attribution was WRONG: the flip is the symptom of a per-vertex displacement overshoot, and native abs-volume repair does NOT stabilize the gate (it converts eversion→inflation). Superseded by [[native-instability-is-displacement-overshoot]]
- [Phase 2 sorting done](phase2-sorting-partial.md) — pixi run sort demixes; SCALE-UP (N=5/dt=1e-4) gives demixing index D −0.043→−0.098 (2.3×), het 460→447 vs pinned frozen, clean stability; new metrics.demixing_index + tests (pixi run test green 11); KEY: dt must scale DOWN with block density; demix still partial (greedy gate local optimum)
- [Phase 1 reconnection done](phase1-reconnection-done.md) — I↔H 3D-T1 built & provably reversible (pixi run test green 6/6); Strategy A manual surgery; one-directional TF op seams
- [Phase 0 baseline control](phase0-baseline-control.md) — control done; Voronoi cluster jams (het neighbor pairs frozen) without reconnection
- [TF headless init](tf-headless-init.md) — windowless=True required; tf.init() is one-per-process (pytest implication)
- [TF MeshQuality default-on](tf-mesh-quality-default-on.md) — auto-enabled; only 3D ops are degenerate collapses; disable via mesh.quality=None
- [pyvoro dispersion gotcha](pyvoro-dispersion-gotcha.md) — set dispersion ≥ seed spacing or you get overlapping full-box cells
- [py3.11 source-build migration](py311-source-build-migration.md) — from-source TF build at py3.11; renderer Patch A (clip)/B (visibility) DONE + pushed to fork
- [vertex render color gotchas](vertex-render-color-gotchas.md) — colour set at surface creation (become doesn't re-render); the "2 BodyTypes clobber" + "camera move resets" claims were DEBUNKED 2026-05-29
- [vertex destroy orphans surfaces](vertex-destroy-orphans-surfaces.md) — Body.destroy() leaves surfaces alive & still rendering; destroy orphaned surfaces (or build only what you render) to hide cells
